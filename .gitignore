/target

# Acurast CLI scratch (logs + staged deploy bundles)
.acurast/

# Secrets — never commit. See e2e/acurast-signer/.env.example
.env
**/.env

node_modules/

# Python bytecode — the Acurast bundler zips fileUrl verbatim, no ignore support
__pycache__/
*.pyc
